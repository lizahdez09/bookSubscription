package MonthlySubscription;

public class Book {
    //A class to represent individual books that are included in the subscription boxes. Each book object can have attributes such as title, author, description, genre, and a unique identifier.
}
