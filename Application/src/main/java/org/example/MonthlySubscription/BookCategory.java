package org.example.MonthlySubscription;

public class BookCategory {
    //A class to define the different book categories (fantasy, romance, sci-fi, etc.). Each category object can have attributes like the category name and a list of books that belong to that category.
}
