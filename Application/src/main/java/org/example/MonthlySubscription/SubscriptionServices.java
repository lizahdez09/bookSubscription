package org.example.MonthlySubscription;

public class SubscriptionServices {
    //A class that manages the subscription service, handling tasks like adding subscribers, selecting books based on their preferred categories, and sending out the subscription boxes regularly.
}
